spring:
  application:
    name: product-service

  main:
    allow-bean-definition-overriding: true

  datasource:
    url: ${DBMS_CONNECTION:jdbc:postgresql://localhost:5433/product_db}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:14062003}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 2

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:/db/changelog-master.xml

  doc:
    swagger-ui:
      path: /swagger-doc/product/swagger-ui.html
    api-docs:
      path: /swagger-doc/v3/api-docs/product

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWK_URI:http://localhost:8180/realms/IamService}

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:admin}
    password: ${RABBITMQ_PASS:admin}

rabbitmq:
  exchange:
    product: product.exchange
    product-dlx: product.exchange.dlx
    file: file.exchange
  queue:
    product:
      update-variant: product.queue.update-variant
      dlq: product.queue.dlq
  routing:
    product:
      update-variant: product.update-variant
      dlk: product.dlk
    key:
      product:
        create: product.create
        update: product.update
        delete: product.delete
        update-all: product.update-all
      file:
        update: file.update

server:
  port: ${SERVER_PORT:8765}

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true